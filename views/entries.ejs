<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-Blog Entries</title>

    <link rel="icon" type="image/webp" href="/pictures/icon.webp">
    <link rel="stylesheet" href="/styles/style.css">
    <script src="/scripts/validation.js"></script>



</head>
<div>
<body>
    <h1>All Posts</h1>
    <% if (!Array.isArray(entries) || entries.length === 0) { %>
        <p>No Posts Found</p>
    <% } else { %>
        <% for (let i=0; i < entries.length; i++) { %>
            <h2><%= entries[i].title %></h2>
            <p><%= entries[i].content %></p>
            <p>Author: <%= entries[i].author %></p>
        <% } %>
    <% } %>
    <p><a href="/"><button>Create a New Post </button></a> <!-- <a href="/entries"><button> View Admin Posts </button></a></p> -->
</body>
</div>
</html>